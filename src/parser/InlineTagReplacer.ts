export default class InlineTagReplacer {

	/*
	private _parseInlineTags(description: string): string {
		const inlineTagsRegex = /{@(.+?) (.+?)}/g;

		description.replace(inlineTagsRegex, this._inlineTagReplacer);
		return "";
	}

	private _inlineTagReplacer(_str: string, tagType: string, data: string): string {
		if (tagType === "link") {

		}
		return "";
	}*/
}
